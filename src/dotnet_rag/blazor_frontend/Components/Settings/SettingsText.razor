<div class="settings-row">
    <div>
        <div class="settings-row-label">@Label</div>
        @if (!string.IsNullOrEmpty(Description))
        {
            <div class="settings-row-desc">@Description</div>
        }
    </div>
    <div class="settings-row-control">
        <input type="text" value="@Value"
               @oninput="OnInput"
               placeholder="@Placeholder"
               style="background:var(--ground);border:1px solid var(--border);border-radius:6px;padding:6px 10px;color:var(--text);font-size:13px;width:100%;outline:none;"
               aria-label="@Label" />
    </div>
</div>

@code {
    [Parameter] public string Label { get; set; } = "";
    [Parameter] public string Description { get; set; } = "";
    [Parameter] public string Value { get; set; } = "";
    [Parameter] public string Placeholder { get; set; } = "";
    [Parameter] public EventCallback<string> ValueChanged { get; set; }

    private Task OnInput(ChangeEventArgs e) => ValueChanged.InvokeAsync(e.Value?.ToString() ?? "");
}
